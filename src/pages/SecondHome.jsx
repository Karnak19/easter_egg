import React from "react"
import ResponsiveLayout from "../Responsive.layout";

const SecondHome = () => {
   return (
      <ResponsiveLayout>
      </ResponsiveLayout>
   );
};

export default SecondHome;
